# dsh-client-ui-archived-tasks bundle patch: inserts the plugin row into the web
# profile roster. The browser half is served through the dsh.client declaration
# in package.json; the host half is an empty apply.
- insert:
    - id: archived-tasks
      name: 'dsh-archived-tasks'
